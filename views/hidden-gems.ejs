<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hidden Gems</title>
    <link rel="stylesheet" href="/styles.css"> <!-- ✅ Ensure CSS is linked -->
</head>
<body>
      <!-- Floating Particles for Sci-Fi Effect -->
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
  

    <div class="xp-bar">
        <div class="xp-bar-fill"></div>
    </div>

    <div class="screen-effect"></div> <!-- CRT Effect -->

    <h1>Forgotten Treasures</h1>
    <%- include('partials/header') %> 
    <p class="neon-subtext">Rarely known, but highly rated.</p>

    <div class="hidden-gems-container">
        <% if (games.length === 0) { %>
            <p class="neon-subtext">No hidden gems found.</p>
        <% } else { %>
            <% games.forEach(game => { %>
                <div class="game-card">
                    <a href="/game-details/<%= game.id %>">
                        <img src="<%= game.img ? game.img : '/images/fallback.jpg' %>"
                            onerror="this.onerror=null; this.src='/images/fallback.jpg';"
                            alt="<%= game.title %>">
                    </a>
                    <h2><%= game.title %> (<%= game.year %>)</h2>
                    <p>⭐ <%= game.rating %>/10 | 🗳️ <%= game.reviews %> reviews</p>
                    <p><%= game.description %></p>
                </div>
            <% }) %>
        <% }; %>
    </div>

    <!-- ✅ Footer Included at the bottom -->
    <%- include('partials/footer') %> 

</body>
</html>
