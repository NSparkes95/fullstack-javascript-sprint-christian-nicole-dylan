<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Most Anticipated Games</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
      <!-- Floating Particles for Sci-Fi Effect -->
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>

    <div class="xp-bar">
        <div class="xp-bar-fill"></div>
    </div>

    <h1>Most Anticipated Games</h1>
    <%- include('partials/header') %> <!-- ✅ Navbar Included -->
    
    <div class="upcoming-container">
        <% if (upcomingGames && upcomingGames.length > 0) { %>
            <% upcomingGames.forEach(game => { %>
                <div class="game-box">
                    <!-- ✅ Link Updated to Upcoming Game Details Page -->
                    <a href="/upcoming-game-details/<%= game.id %>">
                        <img src="<%= game.img %>" alt="<%= game.title %>" width="250">
                        <h2><%= game.title %> (<%= game.releaseDate %>)</h2>
                        <p><%= game.description %></p>
                    </a>
                </div>
            <% }); %>
        <% } else { %>
            <p>No upcoming games found. Check back later for updates!</p>
        <% } %>
    </div>

    <%- include('partials/footer') %>  <!-- ✅ Footer Included -->
</body>
</html>
