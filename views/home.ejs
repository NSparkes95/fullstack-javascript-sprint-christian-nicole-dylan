<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Gameverse Gateway</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

    <h1>Welcome to Gameverse Gateway</h1>
    <%- include('partials/header') %> <!-- ✅ Navbar Included -->

    <div class="random-games-container">
        <% games.forEach(game => { %>
            <div class="game-box">
                <a href="/game-details/<%= game.id %>">
                    <img src="<%= game.img %>" alt="<%= game.title %>" width="250">
                    <h2><%= game.title %> (<%= game.year %>)</h2>
                    <p>⭐ <%= game.rating %>/10</p>
                    <p><%= game.description %></p>
                </a>
            </div>
        <% }); %>
    </div>
    
    <%- include('partials/footer') %>
</body>
</html>
